import { AdminShell } from "@/components/admin-shell";
import { createServicePlan } from "@/lib/actions/service-plans";

const input = "mt-2 w-full rounded-xl border border-black/10 bg-[#f1f0e9] px-4 py-3 text-[13px] outline-none focus:border-[#191b1a]";
export default function NuevoPlan() { return <AdminShell><div className="mx-auto max-w-[850px] px-5 py-8 sm:px-8"><a href="/admin/planes" className="text-[12px] font-semibold text-black/50">← Volver a planes</a><div className="mt-8"><p className="mono text-[9px] uppercase tracking-[.16em] text-black/40">Catálogo</p><h2 className="mt-3 text-4xl font-bold tracking-[-.07em]">Crear un plan</h2><p className="mt-2 text-[13px] text-black/45">El plan queda disponible para asignarlo a nuevos clientes.</p></div><form action={createServicePlan} className="mt-8 rounded-2xl border border-black/10 bg-[#e8e7df] p-5 sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><Field label="Nombre del plan" name="name" required placeholder="Ej. Entrenamiento funcional" className="sm:col-span-2" /><Field label="Descripción" name="description" placeholder="Qué incluye este paquete" className="sm:col-span-2" /><Field label="Precio" name="amount" required inputMode="decimal" placeholder="18000" /><label className="text-[12px] font-semibold">Moneda<select name="currency" className={input}><option value="ARS">Pesos argentinos (ARS)</option></select></label><label className="text-[12px] font-semibold">Frecuencia<select name="frequency" className={input} defaultValue="MONTHLY"><option value="WEEKLY">Semanal</option><option value="BIWEEKLY">Cada 15 días</option><option value="MONTHLY">Mensual</option></select></label><Field label="Día de vencimiento" name="dueDay" type="number" min="0" max="31" defaultValue="10" /></div><div className="mt-7 flex justify-end gap-2"><a href="/admin/planes" className="rounded-full border border-black/10 px-5 py-3 text-[12px] font-semibold">Cancelar</a><button className="rounded-full bg-[#191b1a] px-5 py-3 text-[12px] font-bold text-white">Guardar plan <span className="ml-2">→</span></button></div></form></div></AdminShell> }
function Field({ label, className = "", ...props }: React.InputHTMLAttributes<HTMLInputElement> & { label: string; className?: string }) { return <label className={`text-[12px] font-semibold ${className}`}>{label}<input className={input} {...props} /></label>; }
